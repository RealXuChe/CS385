import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "im.xuche.cs385",
  appName: "Andromeda toolkits",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
